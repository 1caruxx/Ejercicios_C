#include <stdio.h>
#include <stdlib.h>

int main()
{
    int cont,min,max,acum=0,agua;

    float prom;
    for (cont=0 ; cont<30 ; cont++)
    {
         printf("Introduzca la cantidad de agua");
         scanf("&d", &agua);
         acum= acum+agua;
         if (cont==0 || )
    }

    return 0;
}
