#include <stdio.h>
#include <stdlib.h>

int main()
{
  int i;
    printf("Los numeros entre 100 y 0 son:");

    for (i=100 ; i>=0 && i<=100 ; i--)
     {
        printf(" %d",i);
     }
    return 0;
}
