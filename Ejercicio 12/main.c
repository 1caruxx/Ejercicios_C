#include <stdio.h>
#include <stdlib.h>

int main()
{
  int i;
    printf("Los numeros entre 0 y 100 son:");

    for (i=0 ; i>=0 && i<=100 ; i++)
     {
        printf(" %d",i);
     }

    return 0;
}
